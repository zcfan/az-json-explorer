[English](CHANGELOG.md)

# 更新日志

本文件记录 AZ JSON Explorer 已发布的版本，仅在版本发布时更新。

## 0.1.11 — 2026-07-28

- 新增本地化“使用技巧”指南，介绍高效的快捷键与粘贴工作流、输入框缩放、独立视图和项目集成。
- 为扩展入口和查看器新增完整的英文与简体中文本地化，并自动跟随 Chrome 的界面语言。
- JSON 手动输入框支持纵向拖拽调整高度，刷新后恢复高度，并限制为最多占用视口高度的 30%。
- 刷新后恢复历史记录面板的开关状态和宽度，将其限制为最多占用一半视口宽度，并在空白内容区新增点击恢复最近一条历史记录的入口。
- 嵌入式 JSON 页面隐藏独立查看器专属的输入控件；可编辑控件保留原生全选行为，并恢复视图标签页底部的横向联通线。
- 使用全局快捷键打开查看器时，确保 Chrome 窗口切换到前台。

## 0.1.10 — 2026-07-27

- 点击扩展图标或使用全局快捷键 `Cmd/Ctrl+Shift+6`，直接打开独立查看器。
- 无需先聚焦输入框，即可粘贴、全选或显示/隐藏 JSON 输入框；重定向粘贴现在会替换原有输入并立即解析。
- 新增帮助菜单，提供分组的键盘快捷键说明和更新日志入口。
- 每个新版本首次打开时显示一次更新提示，包含 10 秒进度指示器和更新内容链接。

## 0.1.9 — 2026-07-25

- 新增独立视图标签页，可在不离开完整文档的情况下打开对象、数组和字符串。
- 为嵌套 JSON 字符串新增标签页级别的原始和已解析视图。
- 新增持久化解析历史记录，支持恢复、保留数量设置和清理。
- 优化历史记录交互、标签页状态和标签页边框。

## 0.1.8 — 2026-07-23

- 新增分页只读查看器，用于查看完整的超长字符串值。
- 新增行号、完整值复制和完整字符串搜索，且无需将整个字符串传输到界面线程。

## 0.1.7 — 2026-07-17

- 修复嵌套 JSON 字符串在原始与已解析视图之间切换后，搜索结果未刷新的问题。

## 0.1.6 — 2026-07-17

- 新增对已解析嵌套 JSON 字符串的搜索支持。

## 0.1.5 — 2026-07-16

- 新增公共集成，可从网页和其他 Chrome 扩展中打开 JSON。
- 通过后台 Worker 传递启动数据，移除外部启动的数据大小限制。
- 当扩展不可用时，为集成客户端新增安装页面回退流程。

## 0.1.4 — 2026-07-16

- 为独立查看器新增粘贴、解析和搜索快捷键。
- 扩展行右键菜单中的复制和子树操作。
- 与可展开行交互时保留文本选择。
- 重新组织查看器控件，以便更快访问搜索。

## 0.1.3 — 2026-07-14

- 新增点击行展开和收起。
- 新增小型 JSON 文档自动全部展开。

## 0.1.2 — 2026-07-13

- 新增由 Worker 支持的“全部展开”，使用紧凑的展开状态处理大型树。
- 改进独立查看器的大文件性能提示，并支持关闭该提示。

## 0.1.1 — 2026-07-07

- 为独立查看器新增手动 JSON 输入和格式化。
- 为已解析 JSON 字符串的后代节点新增解析感知的 JavaScript 复制路径。
- 优化 Chrome 应用商店介绍和安装说明。

## 0.1.0 — 2026-07-05

- 发布首个 Chrome MV3 扩展版本。
- 新增原始 JSON 页面自动检测和嵌入式树形查看器。
- 新增支持本地文件加载的独立查看器。
- 新增基于 Worker 的解析和搜索、虚拟化行及嵌套 JSON 字符串解析。
- 所有 JSON 处理均保留在本地，不使用后端或遥测。
